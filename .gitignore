node_modules
dist
dist-ssr
.wrangler
*.local
*.log
*.tsbuildinfo
.DS_Store

# Local model mirror for offline dev (npm run fetch-models). Never committed —
# in production every weight streams from a CDN and is cached in the visitor's browser.
public/models/

# LLM weights data file: the 237MB GGUF streams from the HF Hub in prod; only the local dev mirror is ignored.
public/llm/Bonsai-1.7B-Q1_0.gguf

# headless eval Chrome profile (OPFS model cache)
.cache/
